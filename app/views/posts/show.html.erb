<p><%= @post.title %></p>
<p><%= attachment_image_tag @post, :image %></p>

<% if @post.user_id == current_user.id %>
<%= link_to "編集", edit_post_path(@post) %>
<%= link_to "削除", post_path(@post), method: :delete %>
<% end %>
<p><%= @post.title %></p>
<p><%= link_to "#{@post.title}-PDF", rails_blob_path(@post.pdf_file) %></p>

<% if @post.user_id == current_user.id || current_user.admin? %>
<%# <%= link_to "編集", edit_post_path(@post) %>
<%= link_to "削除", post_path(@post), method: :delete %>
<% end %>